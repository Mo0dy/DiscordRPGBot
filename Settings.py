

# the frequency with which the main game loop runs
loop_freq = 20

# the prefix with which the bot is called
command_prefix = "."

# the default hero stats
default_stats = {
    "damage": 10,
    "armor": 5,
}


